---
title: Diagnostics
description: Publicly documented addon investigation and validation tools.
---

import MermaidDiagram from '../../../components/diagrams/MermaidDiagram.astro';
import { buildDiagnosticsDiagram } from '../../../components/diagrams/technicalDiagramBuilders.mjs';

<MermaidDiagram
  label="Investigation stage classifier"
  definition={buildDiagnosticsDiagram('root')}
/>

## Addon Investigation

Use the repo's preferred addon probe command when investigating native UI behavior:

```text
/egloaddonprobe <addon>
```

That is the first tool to reach for when a specific addon family behaves differently than expected.

For hover-tooltip surfaces in `DEBUG` builds, there is also a temporary focused logger:

```text
/eglotooltipregisterlogging <surface|all> [duration]
/eglotooltipregisterlogging stop
```

Use it when you need to confirm:

- which surface actually registered the hover target
- which anchor kind was used
- what bounds were published for the hit area
- when hover-enter and hover-exit transitions occurred

## Build And Test Validation

Standard plugin validation:

```powershell
dotnet build .\Echoglossian.sln -c Debug --no-restore
dotnet test .\Echoglossian.Tests\Echoglossian.Tests.csproj -c Debug --no-build
```

Docs-site validation:

```powershell
Set-Location .\Echoglossian.Docs
npm.cmd run test:locales
npm.cmd run check
npm.cmd run build
```

## Diagnostic Principles

- prefer lifecycle helpers and focused probes over hot-path debug spam
- remove temporary investigation logging once the issue is understood
- classify runtime issues by stage before changing code
- inspect `Echoglossian.log` first; use `dalamud.log` mainly when cross-plugin context is needed
